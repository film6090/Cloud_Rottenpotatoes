#login
  - if @current_user
    %p.welcome  Welcome, #{@current_user.name}!
    = link_to 'Log Out', logout_path, :method => :destroy 
  - else
    %p.login= link_to "Log In with Google", "/auth/google_oauth2", method: :post
